/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    // autoprefixer: {}, // Add this to enable automatic vendor prefixing
  },
};

export default config;
